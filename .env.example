# Workspace vault (scanned by backend)
# New users: point at your knowledge folder (create it first: mkdir -p ~/knowledge)
WORKSPACE_ROOT=~/knowledge

# Existing Hermes users: uncomment instead of ~/knowledge (do not copy this file over .env)
# WORKSPACE_ROOT=~/.hermes/workspaces

# SQLite graph database
DB_PATH=backend/library.db

# Comma-separated browser origins for CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Scan limits
SCAN_MAX_FILES=5000
SCAN_MAX_CHILDREN=50

# Vault hygiene: archive/backups/snapshots folders (exact name match)
# exclude = skip subtree (default) | shadow = folder node only, no children | include = full scan
LGB_ARCHIVE_POLICY=exclude
# Extra directory names to skip (comma-separated), merged with built-in excludes
LGB_EXTRA_EXCLUDE_DIRS=

# Co-located edges: 0 = full clique (≤20 files/folder); N>0 = star hub when folder has >N files
LGB_COLOCATED_MAX=0

# Optional: require X-API-Key on mutating HTTP endpoints when set
LGB_API_KEY=

# Max POST mutations per client IP per minute (rescan, imports, create, etc.)
LGB_RATE_LIMIT_PER_MIN=30

# Hide exception details in HTTP 500 responses when true
LGB_PRODUCTION=

# MCP: allow open_in_os tool (unset = disabled)
LGB_MCP_ALLOW_OS_OPEN=

# Import download limits
LGB_DOWNLOAD_TIMEOUT=120
LGB_MAX_ZIP_BYTES=104857600

# arXiv API (use export.arxiv.org; include contact in User-Agent per arXiv ToU)
# Rate limit: ~1 request per 3 seconds — avoid rapid search/import bursts
LGB_ARXIV_API_BASE=https://export.arxiv.org/api/query
LGB_ARXIV_USER_AGENT=LaGranBiblioteca/1.0 (+https://github.com/Grizaceo/la-gran-biblioteca; mailto:you@example.com)

# Frontend dev proxy target
VITE_API_TARGET=http://127.0.0.1:3001

# Optional: send X-API-Key from browser (must match LGB_API_KEY)
VITE_LGB_API_KEY=
